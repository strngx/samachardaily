---
pagination:
  data: collections.pagedCategoryArticles
  size: 1
  alias: pagedData
permalink: "{{ pagedData.permalink }}"
layout: layouts/category.njk
eleventyComputed:
  title: "{{ pagedData.category.name }}{% if pagedData.pageNumber > 1 %} — Page {{ pagedData.pageNumber }}{% endif %} — Verified News & Explainers"
  description: "{{ pagedData.category.description }}"
---
