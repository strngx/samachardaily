{# VideoObject structured data is intentionally disabled on standard text news articles.
   Google requires VideoObject to only be present on dedicated watch pages where the video
   is the primary content. The visible YouTube video embed remains active via video-embed.njk. #}
